const Audit = () => {
  return (
    <div>
      <h1 className="text-xl font-semibold mb-4">Audit Log</h1>
      <p className="text-sm text-muted-foreground">
        Read-only audit trail of all routing decisions and overrides.
      </p>
    </div>
  );
};

export default Audit;
