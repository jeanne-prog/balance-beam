const Targets = () => {
  return (
    <div>
      <h1 className="text-xl font-semibold mb-4">Flow Targets</h1>
      <p className="text-sm text-muted-foreground">
        Set target volume percentages per provider (must sum to 100%).
      </p>
    </div>
  );
};

export default Targets;
